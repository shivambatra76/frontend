const checkValid = ({ data, type }) => {
    switch (type) {
        case "email":
            /^[a-zA-Z0-9]+@(?:[a-zA-Z0-9]+\.)+[A-Za-z]+$/.test(data);
            break;
        case y:
            // code block
            break;
    }
};